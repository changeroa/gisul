# Direct Worker production verification — 2026-09-21

- Deployed runtime commit: `38677f878c13653954f6183ffafa371d5d3a2590`.
- Worker: `gisul-mcp`, 100% version `97c38524-fcbb-46cc-90ab-8d5ddbdab61c`.
- Previous rollback version: `49b63e31-0b2d-42eb-8aab-006a7864ebf6`.
- Endpoint: https://gisul-mcp.changeroa.workers.dev/mcp
- Canonical source: `/Users/victor/dev-tools/gisul`.
- Permanent deployed checkout: `/Users/iyen/dev-tools/gisul-cloudflare-production` on `macmini`, detached at the runtime commit. Deployment used that host's existing Cloudflare authentication.
- Content: `231b89c37b45337cfee78fa9b2070ea46ecda476`, release `20260921.17`, 33 skills.
- Inventory digest: `sha256:5298a88872e851c35dcd15f0a1ea635e13fca1777a3c1a0df88e6b73278364f5`.
- Installed local plugin: `0.1.0+codex.20260921043512`, source `/Users/victor/plugins/gisul`, using stable HTTPS with a private token file.

## Verification

Worker tests: 36 passed. Server/bridge tests: 45 passed. Worker typecheck and LSP diagnostics passed. Real modern discovery reports `2026-07-28` and private 30-second cache hints. Integration tests verify header mismatch errors, unsupported versions, origin checks, and immutable reads across a release promotion.

Both candidate and stable production passed actual installed-plugin search, load, manifest digest verification, and supporting-file reads. Synthetic Langfuse integration evidence was read back successfully, with six observations and no duplicated observations on a repeated exporter invocation. This is an integration check, not a behavioral model evaluation. Detailed receipts remain in `.deployment-evidence/` in the canonical checkout.

[GitHub Actions run 35561431068](https://github.com/changeroa/gisul-skills/actions/runs/35561431068) passed validation and the Ubuntu-hosted publication job. Its publication receipt is `unchanged`: main already matches the current immutable R2 release, so this run correctly made no new content/pointer write. Existing successful publication run 35550666813 establishes the prior real publication. The workflow does not depend on Mac mini.

Serving path: Codex bridge → HTTPS Worker → private R2. Content updates: gisul-skills main → gated GitHub Actions → Worker publication API → R2. Mac mini is a Wrangler deployment workstation only. The old Node service was retained and is outside these production paths. Existing Codex sessions need a fresh thread/session to load the new plugin transport.
